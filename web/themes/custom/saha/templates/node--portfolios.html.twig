{# 
blog layout: stander, grid, mansory
post single: single style1, single style2. #}

{% set imageoneURL = file_url(node.field_image.0.entity.uri.value) %}
{% set post_format = node.field_portfolios_layout.0.value %}
{% set length_image = node.field_image|length %}
{% set length_image  = length_image - 1 %}

{% if not page %}

	
	{% set array_por = node.field_portfolios_categories  %}
		   
    {% block nodeheader %}
    	
	{% if por_style == 'portfolio1' %}
	    <div  class="maso-item col-md-4 {% for item in  array_por %} {% set tid = item.entity.tid.value %}{{'cat' ~ tid ~ ' '}}{% endfor %} ">
		
		   <div class="img-box adv-img adv-img-down-text boxed-inverse">
		      <a class="img-box img-scale-up i-center" href="{{ url }}">
		         <div class="caption">
		            <i class="fa fa-camera-retro"></i>
		         </div>
		         {{content.field_image.0}}
		      </a>
		      <div class="caption-bottom">
		         <h2><a href="{{ url }}">{{node.getTitle}}</a></h2>
		         <p>{{content.field_subtitle}}</p>
		      </div>
		   </div>
		</div>		

	{% elseif por_style == 'portfolio2' %}

		<div  class="maso-item col-md-4 row-12 {% for item in  array_por %} {% set tid = item.entity.tid.value %}{{'cat' ~ tid ~ ' '}}{% endfor %} ">
		   <div class="img-box adv-img adv-img-classic-box white">
		      <a class="img-box mfp-inline" href="{{url}}">
		      {{content.field_image.0}}
		      </a>
		      <div class="caption">
		         <div class="caption-inner">
		            <a href="{{url}}"> <h2>{{node.getTitle}}</h2></a>
		            <p class="sub-text">{{content.field_portfolios_categories}}</p>
		            <p class="big-text">
		               {{ node.body.summary|length > 292 ? node.body.summary|slice(0,292) ~ '...' : node.body.summary }}
		            </p>
		         </div>
		      </div>
		   </div>
		</div>	

	{% elseif por_style == 'portfolio3' %}

		<div  class="maso-item col-md-4 row-12 {% for item in  array_por %} {% set tid = item.entity.tid.value %}{{'cat' ~ tid ~ ' '}}{% endfor %} ">
		   <div class="img-box adv-img adv-img-full-content" data-anima="fade-left" data-trigger="hover" data-anima-out="hide">
		      <a href="{{imageoneURL}}" class=" anima-scale-up anima">
		      {{content.field_image.0}}
		      </a>
		      <div class="caption-bg anima">
		         <div class="caption">
		            <div class="inner">
		               <i class="fa fa-camera-retro main-icon anima"></i>
		               <a href="{{url}}"><h2>{{node.getTitle}}</h2></a>
		               <p>{{content.field_portfolios_categories}}</p>
		            </div>
		         </div>
		      </div>
		   </div>
		</div>

	{% else %}

		<div  class="maso-item col-md-4 {% for item in  array_por %} {% set tid = item.entity.tid.value %}{{'cat' ~ tid ~ ' '}}{% endfor %} ">
		   <div class="img-box adv-img adv-img-down-text">
		      <a class="img-box img-scale-up i-center" href="{{url}}">
		         <div class="caption">
		            <i class="fa fa-camera-retro"></i>
		         </div>
		        {{content.field_image.0}}
		      </a>
		      <div class="caption-bottom">
		         <h2><a href="{{ url }}">{{ node.getTitle }}</a></h2>
		         <p>{{content.field_subtitle}}</p>
		      </div>
		   </div>
		</div>

	{% endif %}
    
    {% endblock %}
        
{% else %}{# single #}

	<div class="section-empty">
        <div class="container content">
		     {% set por_single_layout = node.field_portfolios_layout.0.value %}
		     {% if por_single_layout == 'layout1' %}

		            <div class="row">
		                <div class="col-md-8">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>{{'DESCRIPCIÓN'|t}}</h2>
		                        <hr class="anima" />
		                    </div>
		                    {{content.body}}
		                </div>
		                <div class="col-md-4">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>{{'DETALLES'|t}}</h2>
		                        <hr class="anima" />
		                    </div>
		                    <ul class="list-texts">

		                        {% if content.field_client is not empty and node.field_client.0.value is not null %}<li><b>{{content.field_client['#title']}}</b>   {{content.field_client}}</li>{% endif %}

		                        {% if node.field_status.0.value is not null and content.field_status is not empty %}<li><b>{{content.field_status['#title']}}</b>   {{content.field_status}}</li>{% endif %}

		                        {% if node.field_location.0.value is not null and content.field_location is not empty %}<li><b>{{content.field_location['#title']}}</b>   {{content.field_location}}</li>{% endif %}

		                        {% if node.field_surface_area.0.value is not null and content.field_surface_area is not empty %}<li><b>{{content.field_surface_area['#title']}}</b> {{content.field_surface_area}}</li>{% endif %}

		                        {% if node.field_date.0.value is not null and content.field_date is not empty %}<li><b>{{content.field_date['#title']}}</b>   {{content.field_date}}</li>{% endif %}

		                        {% if node.field_value.0.value is not null and content.field_value is not empty %}<li><b>{{content.field_value['#title']}}</b> {{content.field_value}}</li>{% endif %}

		                        {% if node.field_architect.0.value is not null and conent.field_architect is not empty %}<li><b>{{content.field_architect['#title']}}</b>   {{content.field_architect}}</li>{% endif %}

		                        {% if node.field_copyright.0.valule is not null and content.field_copyright is not empty %}<li><b>{{content.field_copyright['#title']}}</b>   {{content.field_copyright}}</li>{% endif %}
		                    </ul>
		                    <hr class="space xs" />
		                    {% if node.field_link.0 %}
			                    <a href="{{node.field_link.0.uri}}" class="anima-button btn-sm circle-button"><i class="fa fa-long-arrow-right"></i> {{content.field_link['#title']}}</a>
			                {% endif %}
		                </div>
		            </div>
		            <hr class="space" />
		            <div class="flexslider slider nav-middle-mobile" id="slider-demo-5" data-options="controlNav:false,animationLoop:false,slideshow:false,sync:#slider-demo-5-nav-thumb">
		                <ul class="slides">
		                	
		                	 {% for i in 0..length_image %}
		                	 	<li>
		                        {{content.field_image[i]}}
		                    </li>
		                	 {% endfor %}
		                    
		                    
		                </ul>
		            </div>
		            <div id="slider-demo-5-nav-thumb" class="flexslider carousel nav-thumb nav-middle-mobile" data-options="controlNav:false,animationLoop:false,slideshow:false,asNavFor:#slider-demo-5,minWidth:50,numItems:5">
		                <ul class="slides">
		                  	
		                	 {% for i in 0..length_image %}
		                	 	<li>
		                        {{content.field_image[i]}}
		                    </li>
		                	 {% endfor %}
		                    
		                </ul>
		            </div>
		            <hr class="space" />
		            <div class="row porfolio-bar">
		            	 <div class="col-md-2">
				    		{{prev_por|t}}
				    	</div>
		                <div class="col-md-8 text-center">
		                   
		                </div>
		                <div class="col-md-2 text-right">
			                {{next_por|t}}
			            </div>

		            </div>
		    	
		     {% elseif por_single_layout == 'layout2' %}

		            <div class="row">
		                <div class="col-md-8">
		                    <div class="grid-list gallery">
		                        <div class="grid-box row" data-lightbox-anima="fade-bottom">
		                           
		                           {% for i in 0..length_image %}
		                            	{% set uri_img = node.field_image[i].entity.uri.value %}
				                	 	 <div class="grid-item">
				                	 	 	 <a class="img-box i-center img-scale-rotate" href="{{file_url(uri_img)}}">
					                	 	 	 <i class="fa fa-camera"></i>
					                       		 {{content.field_image[i]}}
				                       		 </a>
				                   	    </div>
				                	 {% endfor %}
		                           
		                            
		                        </div>
		                    </div>
		                </div>
		                <div class="col-md-4 portfolio-details fixed-area" data-topscroll="0" data-bottom="50">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>{{'DETAILS'|t}}</h2>
		                        <hr class="anima" />
		                    </div>
		                    {{content.body}}
		                    <hr />
		                    <ul class="fa-ul ">
		                       {% if content.field_client is not empty and node.field_client.0.value is not null %}<li><b>{{content.field_client['#title']}}</b>   {{content.field_client}}</li>{% endif %}

		                        {% if node.field_status.0.value is not null and content.field_status is not empty %}<li><b>{{content.field_status['#title']}}</b>   {{content.field_status}}</li>{% endif %}

		                        {% if node.field_location.0.value is not null and content.field_location is not empty %}<li><b>{{content.field_location['#title']}}</b>   {{content.field_location}}</li>{% endif %}

		                        {% if node.field_surface_area.0.value is not null and content.field_surface_area is not empty %}<li><b>{{content.field_surface_area['#title']}}</b> {{content.field_surface_area}}</li>{% endif %}

		                        {% if node.field_date.0.value is not null and content.field_date is not empty %}<li><b>{{content.field_date['#title']}}</b>   {{content.field_date}}</li>{% endif %}

		                        {% if node.field_value.0.value is not null and content.field_value is not empty %}<li><b>{{content.field_value['#title']}}</b> {{content.field_value}}</li>{% endif %}

		                        {% if node.field_architect.0.value is not null and conent.field_architect is not empty %}<li><b>{{content.field_architect['#title']}}</b>   {{content.field_architect}}</li>{% endif %}

		                        {% if node.field_copyright.0.valule is not null and content.field_copyright is not empty %}<li><b>{{content.field_copyright['#title']}}</b>   {{content.field_copyright}}</li>{% endif %}
		                    </ul>
		                    <hr class="space xs" />
		                    {% if node.field_link.0 %}
			                    <a href="{{node.field_link.0.uri}}" class="anima-button btn-sm circle-button"><i class="fa fa-long-arrow-right"></i> {{content.field_link['#title']}}</a>
			                {% endif %}
		                    <hr />
		                    
		                    {{social_network_post|t}}

		                </div>
		            </div>
		            <hr class="space" />
		            <div class="row porfolio-bar">
		                
		                <div class="col-md-2">
				    		{{prev_por|t}}
				    	</div>
		                <div class="col-md-8 text-center">
		                   
		                </div>
		                <div class="col-md-2 text-right">
			                {{next_por|t}}
			            </div>
		                
		            </div>
		    	
		     {% elseif por_single_layout == 'layout3' %}

		            <div class="row">
		                <div class="col-md-8">
		                    <div class="flexslider slider white always-visible-nav">
		                        <ul class="slides">
		                             {% for i in 0..length_image %}
		                             	{% set uri_img = node.field_image[i].entity.uri.value %}
				                	 	<li>
				                	 		<a class="img-box lightbox" href="{{file_url(uri_img)}}" data-lightbox-anima="fade-bottom">
				                       		 {{content.field_image[i]}}
				                       		</a>
				                   		</li>
				                	 {% endfor %}
		                          
		                        </ul>
		                    </div>
		                </div>
		                <div class="col-md-4 portfolio-details">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>DETAILS</h2>
		                        <hr class="anima" />
		                    </div>
		                    <ul class="fa-ul ">
		                        {% if content.field_client is not empty and node.field_client.0.value is not null %}<li><b>{{content.field_client['#title']}}</b>   {{content.field_client}}</li>{% endif %}

		                        {% if node.field_status.0.value is not null and content.field_status is not empty %}<li><b>{{content.field_status['#title']}}</b>   {{content.field_status}}</li>{% endif %}

		                        {% if node.field_location.0.value is not null and content.field_location is not empty %}<li><b>{{content.field_location['#title']}}</b>   {{content.field_location}}</li>{% endif %}

		                        {% if node.field_surface_area.0.value is not null and content.field_surface_area is not empty %}<li><b>{{content.field_surface_area['#title']}}</b> {{content.field_surface_area}}</li>{% endif %}

		                        {% if node.field_date.0.value is not null and content.field_date is not empty %}<li><b>{{content.field_date['#title']}}</b>   {{content.field_date}}</li>{% endif %}

		                        {% if node.field_value.0.value is not null and content.field_value is not empty %}<li><b>{{content.field_value['#title']}}</b> {{content.field_value}}</li>{% endif %}

		                        {% if node.field_architect.0.value is not null and conent.field_architect is not empty %}<li><b>{{content.field_architect['#title']}}</b>   {{content.field_architect}}</li>{% endif %}

		                        {% if node.field_copyright.0.valule is not null and content.field_copyright is not empty %}<li><b>{{content.field_copyright['#title']}}</b>   {{content.field_copyright}}</li>{% endif %}
		                    </ul>
		                    <hr class="space xs" />
		                    {% if node.field_link.0 %}
			                    <a href="{{node.field_link.0.uri}}" class="anima-button btn-sm circle-button"><i class="fa fa-long-arrow-right"></i> {{content.field_link['#title']}}</a>
			                {% endif %}
		                </div>
		            </div>
		            <hr class="space m" />
		            <div class="row">
		                <div class="col-md-12">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>INFORMATIONS</h2>
		                        <hr class="anima" />
		                    </div>
		                  {{content.body}}

		                    <hr />
		                    <span>SHARE</span>
		                    <span class="space"></span>
		                  
		                   {{social_network_post|t}}
		                
		                </div>
		            </div>
		            <hr />
		            <div class="row porfolio-bar">
		                
		                 <div class="col-md-2">
				    		{{prev_por|t}}
				    	</div>
		                <div class="col-md-8 text-center">
		                   
		                </div>
		                <div class="col-md-2 text-right">
			                {{next_por|t}}
			            </div>
		                
		            </div>
		    	
		     {% elseif por_single_layout == 'layout4' %}

		            <div class="row">
		                <div class="col-md-8">
		                    <div class="grid-list gallery">
		                        <div class="grid-box row" data-lightbox-anima="show-scale">
		                           
		                            {% for i in 0..length_image %}
		                            	{% set uri_img = node.field_image[i].entity.uri.value %}
				                	 	 <div class="grid-item col-md-4 height-auto">
				                	 	 	 <a class="img-box i-center img-scale-rotate" href="{{file_url(uri_img)}}">
					                	 	 	 <i class="fa fa-camera"></i>
					                       		 {{content.field_image[i]}}
				                       		 </a>
				                   	    </div>
				                	 {% endfor %}
		                           
		                        </div>
		                    </div>
		                </div>
		                <div class="col-md-4 portfolio-details">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>DETAILS</h2>
		                        <hr class="anima" />
		                    </div>
		                    <hr class="space s" />
		                    <ul class="fa-ul ">
		                        {% if content.field_client is not empty and node.field_client.0.value is not null %}<li><b>{{content.field_client['#title']}}</b>   {{content.field_client}}</li>{% endif %}

		                        {% if node.field_status.0.value is not null and content.field_status is not empty %}<li><b>{{content.field_status['#title']}}</b>   {{content.field_status}}</li>{% endif %}

		                        {% if node.field_location.0.value is not null and content.field_location is not empty %}<li><b>{{content.field_location['#title']}}</b>   {{content.field_location}}</li>{% endif %}

		                        {% if node.field_surface_area.0.value is not null and content.field_surface_area is not empty %}<li><b>{{content.field_surface_area['#title']}}</b> {{content.field_surface_area}}</li>{% endif %}

		                        {% if node.field_date.0.value is not null and content.field_date is not empty %}<li><b>{{content.field_date['#title']}}</b>   {{content.field_date}}</li>{% endif %}

		                        {% if node.field_value.0.value is not null and content.field_value is not empty %}<li><b>{{content.field_value['#title']}}</b> {{content.field_value}}</li>{% endif %}

		                        {% if node.field_architect.0.value is not null and conent.field_architect is not empty %}<li><b>{{content.field_architect['#title']}}</b>   {{content.field_architect}}</li>{% endif %}

		                        {% if node.field_copyright.0.valule is not null and content.field_copyright is not empty %}<li><b>{{content.field_copyright['#title']}}</b>   {{content.field_copyright}}</li>{% endif %}
		                    </ul>
		                   <hr class="space xs" />
		                    {% if node.field_link.0 %}
			                    <a href="{{node.field_link.0.uri}}" class="anima-button btn-sm circle-button"><i class="fa fa-long-arrow-right"></i> {{content.field_link['#title']}}</a>
			                {% endif %}
		                </div>
		            </div>
		            <hr class="space m" />
		            <div class="row">
		                <div class="col-md-12">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>INFORMATIONS</h2>
		                        <hr class="anima" />
		                    </div>
		                   {{content.body}}

		                    <hr />
		                    <span>SHARE</span>
		                    <span class="space"></span>
		                    
		                    {{social_network_post|t}}

		                </div>
		            </div>
		            <hr />
		            <div class="row porfolio-bar">
		                
		                 <div class="col-md-2">
				    		{{prev_por|t}}
				    	</div>
		                <div class="col-md-8 text-center">
		                   
		                </div>
		                <div class="col-md-2 text-right">
			                {{next_por|t}}
			            </div>
		                
		            </div>
		     	
		     {% else %}

		            <div class="row">
		                <div class="col-md-8">
		                    <div class="flexslider carousel white">
		                        <ul class="slides">
		                             {% for i in 0..length_image %}
		                             	{% set uri_img = node.field_image[i].entity.uri.value %}
				                	 	<li>
				                	 		<a class="img-box lightbox" href="{{file_url(uri_img)}}" data-lightbox-anima="fade-bottom">
				                       		 {{content.field_image[i]}}
				                       		</a>
				                   		</li>
				                	 {% endfor %}
		                           
		                        </ul>
		                    </div>
		                </div>
		                <div class="col-md-4 portfolio-details">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>DETAILS</h2>
		                        <hr class="anima" />
		                    </div>
		                    <ul class="fa-ul ">
		                        {% if content.field_client is not empty and node.field_client.0.value is not null %}<li><b>{{content.field_client['#title']}}</b>   {{content.field_client}}</li>{% endif %}

		                        {% if node.field_status.0.value is not null and content.field_status is not empty %}<li><b>{{content.field_status['#title']}}</b>   {{content.field_status}}</li>{% endif %}

		                        {% if node.field_location.0.value is not null and content.field_location is not empty %}<li><b>{{content.field_location['#title']}}</b>   {{content.field_location}}</li>{% endif %}

		                        {% if node.field_surface_area.0.value is not null and content.field_surface_area is not empty %}<li><b>{{content.field_surface_area['#title']}}</b> {{content.field_surface_area}}</li>{% endif %}

		                        {% if node.field_date.0.value is not null and content.field_date is not empty %}<li><b>{{content.field_date['#title']}}</b>   {{content.field_date}}</li>{% endif %}

		                        {% if node.field_value.0.value is not null and content.field_value is not empty %}<li><b>{{content.field_value['#title']}}</b> {{content.field_value}}</li>{% endif %}

		                        {% if node.field_architect.0.value is not null and conent.field_architect is not empty %}<li><b>{{content.field_architect['#title']}}</b>   {{content.field_architect}}</li>{% endif %}

		                        {% if node.field_copyright.0.valule is not null and content.field_copyright is not empty %}<li><b>{{content.field_copyright['#title']}}</b>   {{content.field_copyright}}</li>{% endif %}
		                    </ul>
		                    <hr class="space xs" />
		                    {% if node.field_link.0 %}
			                    <a href="{{node.field_link.0.uri}}" class="anima-button btn-sm circle-button"><i class="fa fa-long-arrow-right"></i> {{content.field_link['#title']}}</a>
			                {% endif %}
		                </div>
		            </div>
		            <hr class="space m" />
		            <div class="row">
		                <div class="col-md-12">
		                    <div class="title-base title-small text-left" data-anima="show-scale" data-trigger="hover">
		                        <h2>INFORMATIONS</h2>
		                        <hr class="anima" />
		                    </div>
		                    {{content.body}}

		                    <hr />
		                    <span>SHARE</span>
		                    <span class="space"></span>
		                    
		                    {{social_network_post|t}}

		                </div>
		            </div>
		            <hr />
		            <div class="row porfolio-bar">
		                
		                 <div class="col-md-2">
				    		{{prev_por|t}}
				    	</div>
		                <div class="col-md-8 text-center">
		                   
		                </div>
		                <div class="col-md-2 text-right">
			                {{next_por|t}}
			            </div>
		                
		            </div>
		    	
		     {% endif %}
        </div>
    </div> 
{% endif %}